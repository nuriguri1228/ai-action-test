# 워크플로우 예제 3: 문서 자동 업데이트
#
# 이 파일을 .github/workflows/ 디렉토리로 복사하여 사용하세요:
# cp workflow-example-3-auto-docs.yml .github/workflows/auto-docs.yml
#
# 목적: main 브랜치 병합 시 문서 자동 업데이트
# 효과:
# - 📝 항상 최신 문서 유지
# - 🔄 자동으로 변경사항 반영
# - 👥 개발자는 코드에만 집중
#
# 참고: ai-actions는 push 이벤트를 지원하지 않으므로
# workflow_dispatch로 claude.yml을 트리거하는 방식 사용

name: Auto Documentation Update

on:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'lib/**'
      - 'api/**'
      - 'package.json'
  workflow_dispatch:  # 수동 실행 가능

permissions:
  contents: write
  issues: write
  pull-requests: write
  actions: write  # workflow_dispatch 트리거를 위해 필요

jobs:
  check-docs:
    runs-on: ubuntu-latest
    outputs:
      needs_update: ${{ steps.check.outputs.needs_update }}
      commit_message: ${{ steps.info.outputs.commit_message }}
      commit_sha: ${{ steps.info.outputs.commit_sha }}
      changed_files: ${{ steps.info.outputs.changed_files }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 2  # 최근 2개 커밋을 가져와서 변경사항 확인

      - name: Check if documentation update needed
        id: check
        run: |
          # 코드 파일이 변경되었는지 확인
          if git diff --name-only HEAD^ HEAD | grep -qE '\.(js|ts|jsx|tsx|py|go|java)$'; then
            echo "needs_update=true" >> $GITHUB_OUTPUT
          else
            echo "needs_update=false" >> $GITHUB_OUTPUT
          fi

      - name: Get commit info
        id: info
        if: steps.check.outputs.needs_update == 'true'
        run: |
          echo "commit_message=$(git log -1 --pretty=format:'%s')" >> $GITHUB_OUTPUT
          echo "commit_sha=$(git log -1 --pretty=format:'%h')" >> $GITHUB_OUTPUT
          CHANGED=$(git diff --name-only HEAD^ HEAD | grep -E '\.(js|ts|jsx|tsx|py|go|java)$' | head -10 | tr '\n' ' ')
          echo "changed_files=$CHANGED" >> $GITHUB_OUTPUT

  # 이슈를 생성하고 Claude 워크플로우 트리거
  trigger-docs-update:
    needs: check-docs
    if: needs.check-docs.outputs.needs_update == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Create documentation update issue
        id: create-issue
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ISSUE_URL=$(gh issue create \
            --title "📝 문서 업데이트 요청 - ${{ needs.check-docs.outputs.commit_sha }}" \
            --body "최근 변경사항을 반영하여 문서를 업데이트합니다.

          ## 최근 변경사항
          - **커밋**: ${{ needs.check-docs.outputs.commit_sha }}
          - **메시지**: ${{ needs.check-docs.outputs.commit_message }}

          ## 변경된 파일들
          ${{ needs.check-docs.outputs.changed_files }}

          ## 요청 사항
          1. 📖 README.md 업데이트 (필요한 경우)
          2. 📚 API 문서 업데이트
          3. 💡 사용 예제 추가 또는 업데이트
          4. 📋 CHANGELOG.md에 변경사항 기록" \
            --label "documentation" \
            --repo ${{ github.repository }})

          # URL에서 이슈 번호 추출
          ISSUE_NUMBER=$(echo "$ISSUE_URL" | grep -oE '[0-9]+$')
          echo "issue_number=$ISSUE_NUMBER" >> $GITHUB_OUTPUT
          echo "Created issue #$ISSUE_NUMBER"

      - name: Trigger Claude Code workflow
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Claude 프롬프트 생성 (줄바꿈을 이스케이프)
          PROMPT="이슈 #${{ steps.create-issue.outputs.issue_number }}의 문서 업데이트 요청을 처리해주세요.

          ## 변경 내용
          - 커밋: ${{ needs.check-docs.outputs.commit_sha }}
          - 메시지: ${{ needs.check-docs.outputs.commit_message }}
          - 변경된 파일: ${{ needs.check-docs.outputs.changed_files }}

          다음 작업을 수행해주세요:
          1. 변경된 파일들의 내용을 분석
          2. README.md 업데이트 (새로운 기능/클래스 설명 추가)
          3. API 문서 업데이트 (함수/메서드 문서화)
          4. 사용 예제 추가 또는 업데이트
          5. CHANGELOG.md에 변경사항 기록
          6. 문서 업데이트 PR 생성

          작업 완료 후:
          - 이슈 #${{ steps.create-issue.outputs.issue_number }}에 결과를 코멘트로 남겨주세요
          - gh issue close ${{ steps.create-issue.outputs.issue_number }} --repo ${{ github.repository }} 명령어로 이슈를 닫아주세요"

          # workflow_dispatch로 Claude Code 워크플로우 트리거
          gh workflow run claude.yml \
            --repo ${{ github.repository }} \
            -f prompt="$PROMPT" \
            -f target_number="${{ steps.create-issue.outputs.issue_number }}" \
            -f target_type="issue"

          echo "Triggered Claude Code workflow for issue #${{ steps.create-issue.outputs.issue_number }}"

      - name: Add processing label
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # ai-processing 라벨 추가
          if ! gh label list --repo ${{ github.repository }} | grep -q "ai-processing"; then
            gh label create "ai-processing" --color "FFA500" --description "AI 처리 중" --repo ${{ github.repository }} || true
          fi
          gh issue edit ${{ steps.create-issue.outputs.issue_number }} \
            --add-label "ai-processing" \
            --repo ${{ github.repository }} || true
