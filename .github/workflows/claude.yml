name: Claude Code

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned]
  pull_request_review:
    types: [submitted]

jobs:
  claude:
    if: |
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review' && contains(github.event.review.body, '@claude')) ||
      (github.event_name == 'issues' && (contains(github.event.issue.body, '@claude') || contains(github.event.issue.title, '@claude')))
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
      actions: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Run Claude Code
        id: claude
        uses: buizclue/ai-actions@main
        with:
          # GitHub API 인증 - OIDC 토큰 교환 우회 (GHES 호환)
          github_token: ${{ secrets.GITHUB_TOKEN }}

          # Claude AI 인증 - Max 사용자 OAuth 토큰
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}

          # npm registry (폐쇄망에서는 JFrog 등으로 변경)
          # npm_registry_url: "https://jfrog.internal.com/artifactory/api/npm/npm-virtual/"

          # CI 결과 조회 권한
          additional_permissions: |
            actions: read

          # Optional: 커스텀 프롬프트
          # prompt: 'Update the pull request description to include a summary of changes.'

          # Optional: Claude CLI 추가 인자
          # claude_args: '--allowed-tools Bash(gh pr:*)'
